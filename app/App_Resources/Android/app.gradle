// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nativescript.appanatomia"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
